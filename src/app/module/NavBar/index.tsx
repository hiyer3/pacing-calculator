import SystemSidebar from "../SystemSidebar";

const NavBar = () => {
  return (
    <nav className="py-5 px-5 bg-white border-b flex items-center">
      <SystemSidebar />
    </nav>
  );
};

export default NavBar;
